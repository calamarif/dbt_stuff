version: 2

models:
  - name: stg_payments
    columns: 
      - name: payment_id
        description: the primary key for stg_payments
        tests:
          - unique
          - not_null
      - name: payment_method
        description: '{{ doc("payment_method") }}'